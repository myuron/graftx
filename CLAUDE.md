# CLAUDE.md

## プロジェクト概要

このプロジェクトは、リポジトリ間のコピーを加速させることを目的としたTUIアプリケーションです。
左右にペインを持ち、左ペインはコピー元、右ペインはコピー先です。
基本的にVimバインドでキーボードだけで操作を完結させます。

## ワークフロー

このプロジェクトはテスト駆動開発を採用します。
以下のワークフローに準拠してください。
1. 失敗するテストを書く
2. テストをパスするコードを書く
3. フォーマッター 
4. リンター
5. ユニットテスト
3. リファクタリングする

## 開発言語やライブラリ

### 開発言語
Go

### ライブラリ
jroimartin/gocui

## 規約
- ソースコード内のコメントは日本語で記述すること。
- GitHubリポジトリではmainへの直接Pushを禁止している。必ずブランチを払い出してPRを出すフローを遵守すること。
- 作業を行う際にToDoリストを.claude/plans/plan.mdに出力し、トラッキングしながら作業を行うこと。
- コードベースの探索を行う場合はSerena MCPを用いること。
- ライブラリを使う場合は以下のフローを実施すること。
  - `gomarkdoc` を使いライブラリのドキュメントを `docs/lib/{ライブラリ名}.md` に生成すること。
  - `go get` 実行前には必ずユーザーの許可を得ること。
  - ライブラリの使い方はdocs/lib配下にmdが配置されているので、利用すること。
