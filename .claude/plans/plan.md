# graftx 実装計画

設計ドキュメント: [docs/design.md](../../docs/design.md)

## フェーズ1: 基盤レイヤー

依存グラフの葉ノードから構築し、テスト可能な状態を常に維持する。

- [x] `internal/fs` パッケージの実装
  - [x] `Entry` 構造体の定義
  - [x] `FileSystem` インターフェースの定義
  - [x] `ReadDir` の実装（ディレクトリ先・アルファベット順ソート）
  - [x] `Copy` の実装（ファイル/ディレクトリの再帰コピー）
  - [x] `Remove` の実装
  - [x] `Trash` の実装（macOS `~/.Trash`）
  - [x] `Rename` の実装
  - [x] `Create` の実装
  - [x] ユニットテスト（一時ディレクトリ使用）
- [x] `internal/pane` パッケージの実装
  - [x] `Pane` 構造体の定義
  - [x] `YankBuffer` 構造体の定義
  - [x] カーソル移動（上下、クランプ処理）
  - [x] ディレクトリ遷移（`h`/`l` に対応するロジック、カーソル復元）
  - [x] ファイル選択（トグル、全選択、反転、解除）
  - [x] ヤンク操作（選択中 or カーソル行をバッファに格納）
  - [x] ユニットテスト（`FileSystem` モック注入）

## フェーズ2: 最小TUI表示

起動してディレクトリをブラウジングできる最小構成。

- [x] `internal/selector` パッケージの実装
  - [x] `CommandRunner` インターフェースの定義
  - [x] `ghq list --full-path | fzf` の実行と結果取得
  - [x] ユニットテスト（コマンド実行モック注入）
- [x] `internal/ui` パッケージの実装
  - [x] `App` 構造体・`ExitReason` の定義
  - [x] `Layout` 関数（左右ペイン + ステータスバーの3View生成）
  - [x] 右ペインにカレントディレクトリ表示
  - [x] 左ペインにヒントメッセージ表示
  - [x] フォーカス中ペインの枠線ハイライト
  - [x] ウィンドウリサイズ対応
- [x] `main.go` の実装
  - [x] gocui初期化とMainLoopの再起動ループ
- [x] 基本キーバインドの登録
  - [x] `j`/`k` カーソル移動
  - [x] `h`/`l` ディレクトリ遷移
  - [x] `Tab` ペイン間フォーカス切替
  - [x] `q` 終了

## フェーズ3: コア機能

ユースケース「リポジトリ間コピー」を実現する。

- [ ] `s` キーによるリポジトリ選択
  - [ ] MainLoop一時停止 → fzf起動 → 再初期化の実装
  - [ ] 左ペインに選択リポジトリのファイル一覧表示
  - [ ] キャンセル時の状態復元
- [ ] ファイル選択の描画
  - [ ] `Space` 選択トグルのキーバインドと `*` マーク表示
  - [ ] `v` ビジュアルモード
  - [ ] `Ctrl+a` 全選択、`Ctrl+r` 反転、`Esc` 解除
- [ ] コピー操作
  - [ ] `y` ヤンクのキーバインドとステータスバー表示
  - [ ] `p` ペーストの実装
  - [ ] 同名ファイル確認ダイアログ（ポップアップペイン）
  - [ ] `P` 上書きペースト

## フェーズ4: 補助機能

- [ ] `gg`/`G` 先頭・末尾ジャンプ
- [ ] `.` 隠しファイル表示トグル
- [ ] 削除操作
  - [ ] `d` ゴミ箱移動（確認ダイアログ付き）
  - [ ] `D` 完全削除（確認ダイアログ付き）
- [ ] `r` リネーム（ポップアップペイン）
- [ ] `a` 新規作成（ポップアップペイン）
- [ ] 検索・フィルタ
  - [ ] `/`/`?` 検索
  - [ ] `n`/`N` 次/前の一致
  - [ ] `f` フィルタ
